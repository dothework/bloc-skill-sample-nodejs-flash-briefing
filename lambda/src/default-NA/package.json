{
  "name": "six-voices-today-in-history-skill",
  "version": "1.0.0",
  "description": "today in history skill",
  "main": "index.js",
  "scripts": {
      "invoke": "aws lambda invoke --function-name 'alexaSkillThisDayInHistory' --payload fileb://../tests/events/alexa-start-session.json ../tests/out.txt && cat ../tests/out.txt",
      "test": "node ../tests/test.js",
      "deploy": "npm run zip && npm run upload",
      "upload": "aws lambda update-function-code --function-name 'alexaSkillThisDayInHistory' --zip-file fileb://../index.zip",
      "zip": "zip -r ../index.zip . -x 'package.json' -x '*.git*' -x '*.DS_Store'"
  },
  "keywords": [
    "alexa",
    "skill",
    "fact"
  ],
  "author": "Amazon.com",
  "license": "Apache-2.0",
  "dependencies": {
    "alexa-sdk": "^1.0.12"
  }
}
